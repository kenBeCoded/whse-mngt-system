{
  "name": "whse-mngt-system-root",
  "version": "1.0.0",
  "description": "Warehouse Management System Root",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently --kill-others-on-fail \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:all": "npm run build --prefix backend && npm run build --prefix frontend",
    "docker:up": "docker compose up --watch",
    "docker:down": "docker compose down",
    "docker:build": "docker compose build"
  }
}
