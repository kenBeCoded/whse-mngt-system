info:
  name: List all active warehouses
  type: http
  seq: 2
http:
  method: GET
  url: http://localhost:8000/api/warehouses
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
