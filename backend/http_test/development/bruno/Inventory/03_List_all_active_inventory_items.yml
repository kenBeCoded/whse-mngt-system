info:
  name: List all active inventory items
  type: http
  seq: 3
http:
  method: GET
  url: http://localhost:8000/api/inventory/items
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
